# Copy this into Streamlit Community Cloud: App → Settings → Secrets
# (Or save as `.streamlit/secrets.toml` locally — never commit real values.)

EMAIL_SENDER = "your_gmail@gmail.com"
EMAIL_APP_PASSWORD = "your_16_char_app_password"
EMAIL_RECIPIENT = "alerts_inbox@gmail.com"

# Optional — when you connect a broker for data only
# BROKER = ""
# BROKER_API_KEY = ""
# BROKER_API_SECRET = ""
# BROKER_ACCESS_TOKEN = ""
